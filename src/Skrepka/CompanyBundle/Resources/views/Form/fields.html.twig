{% block form_errors %}
{% spaceless %}
    {% if errors|length > 0 %}
        {% for error in errors %}
            <span class="help-inline alert-danger">{{ error.message }}</span>
        {% endfor %}
    {% endif %}
{% endspaceless %}
{% endblock form_errors %}

{% block form_row %}
    {#col-md-7#}
    <div class="control-group ">
        {{ form_label(form) }}{{ form_errors(form) }}
        <div class="controls">
            {{ form_widget(form, { 'attr': {'class': 'form-control'} }) }}
        </div>
    </div>
{% endblock form_row %}

{% extends 'form_div_layout.html.twig' %}
{% block form_label %}
    {{ parent() }}
    {% if required %}
        <span class="required" style="color:red; margin-left:2px" title="This field is required">*</span>
    {% endif %}
{% endblock %}


