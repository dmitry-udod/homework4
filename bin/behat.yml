default:
    context:
        parameters:
            javascript_session:   sahi
            base_url:             http://localhost/courses/homework4/
            show_cmd:             open %s

    extensions:
        Behat\Symfony2Extension\Extension:
            mink_driver: true
        Behat\MinkExtension\Extension:
            base_url:    'http://localhost/courses/homework4/'
            default_session: sahi
            javascript_session: sahi
            sahi: ~

    paths:
        features: features
    context:
        class: Skrepka\CompanyBundle\Features\Context\FeatureContext